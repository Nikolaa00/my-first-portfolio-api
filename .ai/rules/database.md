---
paths:
  - 'database/**'
---

# Database

## PostgreSQL via Laravel Sail
This project uses PostgreSQL (prime_capital_database) through Laravel Sail. Run migrations and database commands inside the Sail container, not against host SQLite.
