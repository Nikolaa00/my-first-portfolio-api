---
paths:
  - 'app/**'
---

# App

## Flat Laravel skeleton — no extra layers yet
The app uses the default Laravel 13 layout (Http, Models, Providers only). There is no Actions, Services, Repositories, or domain module layer yet. Keep new code in controllers and models until an explicit layer is introduced.
